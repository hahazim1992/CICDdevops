language: node_js

node_js:
  - 12.18.3

build:
  ci:
    # - pwd
    - ssh ec2-user@18.138.250.62 "cd /home/ec2-user/node1 && git pull && pm2 restart index.js" 
    # - pwd
    # - cd ~/cicdshippable
    # - git pull
    # - hostname
    # - npm install
    # - npm install pm2
    # - sudo npm i -g pm2
    # - sudo npm install -g pm2
    # - npm install - production
    